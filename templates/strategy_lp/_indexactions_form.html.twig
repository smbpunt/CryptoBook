<form method="post" action="{{ path('strategy_l_p_delete', {'id': strategy_farming['strategy'].id}) }}"
      onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ strategy_farming['strategy'].id) }}">
    <div class="btn-group" role="group" aria-label="Basic outlined example">
        <a href="{{ path('strategy_l_p_edit', {'id': strategy_farming['strategy'].id}) }}"
           class="btn btn-outline-light">Modifier</a>
        <button type="submit" class="btn btn-outline-light">Supprimer</button>
    </div>
</form>
