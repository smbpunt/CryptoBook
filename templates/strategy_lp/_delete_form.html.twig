<form method="post" action="{{ path('app_strategy_lp_delete', {'id': strategy_l_p.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ strategy_l_p.id) }}">
    <button class="btn">Delete</button>
</form>
