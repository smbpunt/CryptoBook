{% extends 'base.html.twig' %}

{% block title %}{{ parent() }} - Emprunts{% endblock %}

{% block body %}
    <h1>Loan</h1>

    <table class="table table-dark">
        <tbody>
        <tr>
            <th>Id</th>
            <td class="text-white align-middle">{{ loan.id }}</td>
        </tr>
        <tr>
            <th>NbCoins</th>
            <td class="text-white align-middle">{{ loan.nbCoins }}</td>
        </tr>
        </tbody>
    </table>

    <a href="{{ path('loan_index') }}">back to list</a>

    <a href="{{ path('loan_edit', {'id': loan.id}) }}">edit</a>

    {{ include('loan/_delete_form.html.twig') }}
{% endblock %}
