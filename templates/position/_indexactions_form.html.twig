<form method="post" action="{{ path('app_position_delete', {'id': detail.id}) }}"
      onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ detail.id) }}">
    <div class="btn-group" role="group" aria-label="Basic outlined example">
        <a href="{{ path('app_position_edit', {'id': detail.id}) }}" class="btn btn-outline-light">Modifier</a>
        <button type="submit" class="btn btn-outline-light">Supprimer</button>
    </div>
</form>
