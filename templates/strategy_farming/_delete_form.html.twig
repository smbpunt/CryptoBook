<form method="post" action="{{ path('app_strategy_farming_delete', {'id': strategy_farming.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ strategy_farming.id) }}">
    <button class="btn">Delete</button>
</form>
