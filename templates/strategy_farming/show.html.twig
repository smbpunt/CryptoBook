{% extends 'base.html.twig' %}

{% block title %}{{ parent() }} - Farming{% endblock %}

{% block body %}
    <h1>StrategyFarming</h1>

    <table class="table table-bordered table-striped table-responsive-md">
        <tbody>
        <tr>
            <th>Id</th>
            <td>{{ strategy_farming.id }}</td>
        </tr>
        <tr>
            <th>NbCoins</th>
            <td>{{ strategy_farming.nbCoins }}</td>
        </tr>
        <tr>
            <th>Apr</th>
            <td>{{ strategy_farming.apr }}</td>
        </tr>
        </tbody>
    </table>

    <a href="{{ path('strategy_farming_index') }}">back to list</a>

    <a href="{{ path('strategy_farming_edit', {'id': strategy_farming.id}) }}">edit</a>

    {{ include('strategy_farming/_delete_form.html.twig') }}
{% endblock %}
