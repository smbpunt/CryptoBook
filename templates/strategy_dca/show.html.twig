{% extends 'base.html.twig' %}

{% block title %}StrategyDca{% endblock %}

{% block body %}
    <h1>StrategyDca</h1>

    <table class="table table-bordered table-striped table-responsive-md">
        <tbody>
        <tr>
            <th>Id</th>
            <td>{{ strategy_dca.id }}</td>
        </tr>
        <tr>
            <th>FiatToDcaEur</th>
            <td>{{ strategy_dca.fiatToDcaEur }}</td>
        </tr>
        <tr>
            <th>FarmingToDcaUsd</th>
            <td>{{ strategy_dca.farmingToDcaUsd }}</td>
        </tr>
        </tbody>
    </table>

    <a href="{{ path('strategy_dca_index') }}">back to list</a>

    <a href="{{ path('strategy_dca_edit', {'id': strategy_dca.id}) }}">edit</a>

    {{ include('strategy_dca/_delete_form.html.twig') }}
{% endblock %}
