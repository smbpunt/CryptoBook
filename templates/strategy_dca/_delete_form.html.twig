<form method="post" action="{{ path('strategy_dca_delete', {'id': strategy_dca.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ strategy_dca.id) }}">
    <button class="btn">Delete</button>
</form>
