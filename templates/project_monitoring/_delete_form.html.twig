<form method="post" action="{{ path('project_monitoring_delete', {'id': project_monitoring.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ project_monitoring.id) }}">
    <button class="btn">Delete</button>
</form>
