<form method="post" action="{{ path('project_monitoring_delete', {'id': project_monitoring.id}) }}"
      onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ project_monitoring.id) }}">
    <div class="btn-group" role="group" aria-label="Basic outlined example">
        <a href="{{ path('project_monitoring_edit', {'id': project_monitoring.id}) }}" class="btn btn-outline-light">Modifier</a>
        <button type="submit" class="btn btn-outline-light">Supprimer</button>
    </div>
</form>
